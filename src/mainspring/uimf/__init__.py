"""The UIMF data layer: SQLite access, blob decoding, calibration, sparse frames,
rasterisation, and the `uimf-info` command line.

Never imports Qt. Modules are filled in by the lab record's tasks 02 and 03; until
then this package exists so that the layout is fixed and importable.
"""
